# Algolia DocSearch (search-only API key — safe to embed in the built site)
ALGOLIA_APP_ID=
ALGOLIA_API_KEY=
ALGOLIA_INDEX_NAME=essentialsy

# When unset, the wiki falls back to VitePress local search during `npm run dev`.
