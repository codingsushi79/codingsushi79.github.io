# Microsoft Sign-In

Papyrus Client uses the same Microsoft OAuth flow as official Minecraft launchers (via `@xmcl/user`).

- You **cannot launch** without completing sign-in
- Tokens are stored locally in the launcher data folder
- Sign out from the header menu to switch accounts

This ensures only legitimate Java Edition accounts connect to integrity-enabled servers.
